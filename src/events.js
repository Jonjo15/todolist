import { domeElements} from "./dom";



function bindEvents() {
    domeElements.newProjectButton.addEventListener("click", (e) => {
        //create a form
    });

    domeElements.addNewTodoButton.addEventListener("click", (e) => {
        //create a form
    });
}